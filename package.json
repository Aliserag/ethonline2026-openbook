{
  "name": "openbook",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["agent", "mcp", "app"],
  "scripts": {
    "test:agent": "bun test agent",
    "test:mcp": "bun test mcp",
    "test:scripts": "bun test scripts/stale-proxy.test.ts",
    "build:mcp": "cd mcp && bun run build",
    "build:app": "cd app && bun run build",
    "dev:app": "cd app && bun run dev"
  }
}
